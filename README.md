很好，这个 README 已经**信息是对的**，现在主要问题是：

* 结构不够标准（GitHub 展示不友好）
* 代码块没正确闭合
* 项目结构示例不符合仓库实际内容
* 对 **config.example.py / 安全模型** 说明不够清晰
* 少了一点“专业感”和可读性

下面我给你一份 **「优化后的完整 README.md」**，
👉 **你可以直接整体替换现在这个文件**。

---

# ✅ 优化后的 `README.md`（推荐直接使用）

````md
# Telegram Image Bot (Cloudflare R2)

一个 **私用 / 管理员限定** 的 Telegram 图床 Bot。  
使用 **Cloudflare R2** 存储图片，**稳定、无风控、无出站流量费**，适合长期自用。

---

## ✨ 特性

- 🔐 仅允许管理员上传图片（防滥用）
- ☁️ Cloudflare R2 对象存储
- 🔗 自动返回 Direct / Markdown / BBCode 链接
- 🐳 Docker / docker-compose 一键部署
- 🚫 不依赖任何第三方图床服务
- 🧩 代码简洁，易于二次扩展

---

## 📦 项目结构

```text
tg-img-r2-bot/
├── main.py              # Telegram Bot 主程序
├── Dockerfile           # Docker 镜像构建文件
├── docker-compose.yml   # docker-compose 启动配置
├── config.example.py    # 配置示例（无敏感信息）
├── .gitignore
└── README.md
````

> ⚠️ **注意**
> `config.py` 为本地真实配置文件，**只存在于服务器上**，
> 不会、也不应该被提交到 GitHub。

---

## 🚀 快速开始

### 1️⃣ 克隆项目

```bash
git clone https://github.com/yourname/tg-img-r2-bot.git
cd tg-img-r2-bot
```

---

### 2️⃣ 配置 Bot 与 R2

复制示例配置文件并填写真实信息：

```bash
cp config.example.py config.py
nano config.py
```

你需要准备：

* Telegram Bot Token
* Telegram 管理员 user_id
* Cloudflare R2 Access Key / Secret
* R2 Bucket 名称
* R2 绑定的自定义域名

---

### 3️⃣ 构建并运行

```bash
docker build -t tg-img-r2 .
docker-compose up -d
```

---

### 4️⃣ 查看运行日志

```bash
docker logs -f tg-img-bot
```

启动成功后，你会看到类似日志：

```
[BOOT] starting telegram bot
```

---

### 5️⃣ 验证功能

向 **Telegram Bot（管理员账号）** 发送一张图片，
Bot 将返回：

* Direct Link
* Markdown 格式
* BBCode 格式

---

## 🔐 安全说明（非常重要）

* ✅ 仓库中仅保留 `config.example.py`
* 🔑 所有 Token / Key 只存在于服务器本地
* 🔒 Bot 默认拒绝非管理员的上传请求

---

